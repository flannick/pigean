# Legacy scripts

This directory is for frozen/legacy scripts kept for reproducibility.

- `priors.py`: current legacy production script snapshot.

Do not import from `legacy/` in new `src/` code. New code should live under `src/`.
