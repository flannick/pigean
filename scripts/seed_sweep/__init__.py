"""Seed-replication sweep tooling for PIGEAN.

Run the same PIGEAN command under several random seeds, then aggregate the
per-seed stats tables into mean/sd/rank tables so run-to-run stability can be
measured directly.
"""
